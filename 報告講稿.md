# 期末報告講稿 — C 語言貪食蛇 Final

> 對應評分「報告必講重點」四大項。每段都附**該講的話**＋**可指的程式碼**，照著講即可。
>
> 線上展示：https://singpraise.github.io/snake-game-final/ ｜ Repo：https://github.com/singpraise/snake-game-final

---

## 開場（30 秒）

> 「我做的是一個 C 語言貪食蛇，核心是 **Queue（蛇身）、Stack（Undo）、2D 陣列（地圖）** 三種資料結構，全程用 **malloc/free** 手動管理記憶體。同一份 C 程式碼可以在 **Windows 終端機**跑，也能用 Emscripten 編成 **WebAssembly 在瀏覽器**玩。整個專案我用 Cursor 協作，但每一行都親自編譯、執行、除錯驗證過。」

---

## 重點 1：專案整體架構設計

**要講的話：**
> 「我採用**模組化分層**設計，把『資料結構層 / 遊戲邏輯層 / 平台層』完全分離，所以核心邏輯不用改，就能同時支援終端機和網頁。」

**三層架構：**

```
平台層（可替換）   input_win.c / platform_web.c / audio.c
        │  只負責「輸入、輸出、發聲」，與平台相關
遊戲邏輯層         snake.c (game_update)、score.c、obstacles.c、level.c
        │  規則：移動、吃食物、碰撞、計分、Undo
資料結構層         queue.c（蛇身）、stack.c（Undo）、board.c（地圖）、memory.c
        │  純資料結構，與遊戲規則無關，可獨立測試
```

**設計理由（關鍵賣點）：**
- **同一份核心 `.c`** 在 Windows 與 Web 都用，**只換平台層**（`input_win.c` ↔ `platform_web.c`）。
- 每個模組 `.h`/`.c` 分離，標頭檔寫清楚職責與「malloc/free 配對」。
- 一幀流程：`讀輸入 → game_update（移動/碰撞/計分）→ board_rebuild → 繪製`。

> 可指 README 的「系統架構」圖與 `platform.h`（平台抽象介面）。

---

## 重點 2：Pointer 與 Malloc 用法解析

**要講的話：**
> 「我把所有 malloc/free 收斂到一個 `memory.c`，每個 `create` 一定對應一個 `destroy`，而且用 `MEM_DEBUG` 計數驗證沒有 leak。」

**(a) 統一記憶體層 + 洩漏偵測** —— `memory.c`

```10:19:snake/memory.c
void *mem_malloc(size_t size)
{
    void *ptr = malloc(size);
#ifdef MEM_DEBUG
    if (ptr != NULL) {
        s_live_allocs++;
    }
#endif
    return ptr;
}
```

- 每次配置 `s_live_allocs++`，釋放 `--`；結束時 `mem_assert_clean()` 印出是否歸零 → **證明沒有 Memory Leak**。
- `SAFE_FREE(p)`：free 後把指標設回 `NULL`，避免**懸空指標 / double free**。

**(b) 指標傳遞 struct，避免整包複製** —— Queue 用鏈結節點串接

```46:68:snake/queue.c
int queue_enqueue(Queue *q, Point p)
{
    if (q == NULL) {
        return -1;
    }

    QueueNode *node = (QueueNode *)mem_malloc(sizeof(QueueNode));
    if (node == NULL) {
        return -1;
    }
    node->data = p;
    node->next = NULL;

    if (q->rear == NULL) {
        q->front = node;
        q->rear  = node;
    } else {
        q->rear->next = node;
        q->rear = node;
    }
    q->size++;
    return 0;
}
```

**(c) 二重指標 `Cell **` 配置 2D 地圖** —— `board.c`

```44:63:snake/board.c
    /* ② 配置「列指標陣列」：height 個 Cell* */
    board->cells = (Cell **)mem_malloc((size_t)height * sizeof(Cell *));
    if (board->cells == NULL) {
        SAFE_FREE(board);
        return NULL;
    }

    /* ③ 為每一列配置 width 個 Cell */
    for (int y = 0; y < height; y++) {
        board->cells[y] = (Cell *)mem_malloc((size_t)width * sizeof(Cell));
        if (board->cells[y] == NULL) {
            /* 配置失敗：free 已成功的列與 board，遵守黃金法則 */
            for (int i = 0; i < y; i++) {
                SAFE_FREE(board->cells[i]);
            }
            SAFE_FREE(board->cells);
            SAFE_FREE(board);
            return NULL;
        }
        memset(board->cells[y], 0, (size_t)width * sizeof(Cell));
    }
```

> 重點講：`cells` 是 `Cell**`、`cells[y]` 是 `Cell*`、`cells[y][x]` 是格子值；**配置到一半失敗時，會把已配置的部分逐一釋放**，這是處理 malloc 失敗的正確做法。

**(d) 邊界檢查防越界** —— 所有存取都先檢查範圍

```103:112:snake/board.c
Cell board_get(const Board *board, int x, int y)
{
    if (board == NULL || board->cells == NULL) {
        return CELL_EMPTY;
    }
    if (x < 0 || x >= board->width || y < 0 || y >= board->height) {
        return CELL_EMPTY;
    }
    return board->cells[y][x];
}
```

---

## 重點 3：資料結構的應用場景與設計理由

**要講的話：**
> 「我刻意為不同需求選不同結構：蛇身用 **Queue** 因為移動就是『頭進尾出』；Undo 用 **Stack** 因為要『後進先出』還原；地圖用 **2D 陣列** 因為碰撞偵測要 O(1)。」

| 結構 | 用在哪 | 為什麼選它（設計理由） |
|------|--------|------------------------|
| **Queue（FIFO）** | 蛇的身體 | 蛇移動 = **新頭 enqueue、舊尾 dequeue**，天生就是 FIFO；吃到食物時「不 dequeue」就等於變長 |
| **Stack（LIFO）** | Undo 還原 | 每步存一個快照 push，按 U 鍵 pop **最近一步**還原，後進先出正好符合「復原」語意 |
| **2D 陣列** | 遊戲地圖 | `board[y][x]` 直接 O(1) 判斷該格是牆/蛇/食物，碰撞偵測不用遍歷整條蛇 |

**Queue 的「吃食物變長」設計 —— 移動只 dequeue 一次，吃到就不 dequeue：**
> （指 `snake.c` 的 `game_update`：enqueue 新頭後，若該格是食物則保留尾巴、否則 `queue_dequeue` 砍尾。）

**Stack 限制長度避免無限吃記憶體** —— 超過上限丟棄最舊快照：

```107:111:snake/stack.c
    while (s->size > UNDO_STACK_MAX) {
        stack_discard_oldest(s);
    }
```

**快照釋放的所有權設計** —— pop 出來若沒人接手，就自己釋放內部的 body/board：

```13:20:snake/stack.c
void snapshot_free(MoveSnapshot *snap)
{
    if (snap == NULL) {
        return;
    }
    SAFE_DELETE(snap->body, queue_destroy);
    SAFE_DELETE(snap->board, board_destroy);
}
```

---

## 重點 4：你與 Cursor 協作解決了什麼具體問題

**要講的話：**
> 「Cursor 幫我加速了架構發想跟樣板程式，但最有價值的協作是**除錯**。最經典的例子是 GitHub Pages 自動部署，我失敗了 14 次才成功，過程完全靠『讀真實錯誤訊息』而不是亂猜。」

**具體案例：GitHub Actions 部署 14 次失敗 → 第 15 次成功**

| 階段 | 表面現象 | 真正原因 | 解法 |
|------|----------|----------|------|
| 1 | Build 0 秒就失敗 | `build_web.sh` 是 **Windows CRLF 換行**，Linux bash 解析不了 | 加 `.gitattributes` 強制 `.sh` 用 LF |
| 2 | `emcc: command not found` | GitHub Actions **繞過 Docker 映像的 ENTRYPOINT**，環境變數沒載入 | 在步驟內 `source $EMSDK/emsdk_env.sh` |
| 3 | 編譯錯誤終於浮現 | `EM_ASM` 不支援 `-std=c99` | 改用 `-std=gnu99` |
| 4 | `duplicate case value '85'` | `case 'U'` 與 `case 85` 重複（`'U'` 的 ASCII 就是 85） | 移除多餘的 `case 85` |

**這題的學習價值（面試/報告金句）：**
> 「前 12 次失敗我一直在換工具、換 action，其實都在猜。真正的轉折是我讓 CI **把 emcc 的錯誤訊息印出來**，才發現是兩個被環境問題掩蓋的 C 編譯錯誤。**除錯要先讓錯誤可見、分層定位，而不是盲目嘗試。**」

> 完整紀錄見 [`PROMPT_LOG.md`](PROMPT_LOG.md)。

---

## 收尾（20 秒）

> 「總結：這個專案展示了 C 的指標與動態記憶體（malloc/free 配對、二重指標、洩漏偵測）、三種資料結構的合理選用、模組化跨平台架構，以及一段真實的 AI 協作除錯歷程。謝謝。」

---

## 預期問答（Q&A 預備）

| 可能被問 | 怎麼答 |
|----------|--------|
| 怎麼證明沒有 memory leak？ | 開 `MEM_DEBUG`，`mem_assert_clean()` 會印出 live 區塊數，正常結束為 0 |
| 為什麼蛇身不用陣列就好？ | 陣列砍頭/搬移是 O(n)，Queue 鏈結頭尾操作 O(1)，更貼合「頭進尾出」 |
| 地圖為什麼不用一維陣列？ | 一維也行（`y*w+x`），但 `cells[y][x]` 可讀性高，且示範了二重指標配置/釋放 |
| Web 版跟桌面版差在哪？ | 只差平台層（輸入/輸出/音效），核心 `.c` 完全相同，用 Emscripten 編譯 |
| Undo 會不會吃爆記憶體？ | 不會，Stack 有 `UNDO_STACK_MAX` 上限，超過丟棄最舊快照 |
